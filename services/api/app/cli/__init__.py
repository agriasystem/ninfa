"""Internal, operator-run CLI entry points (Gate 13: `python -m app.cli.auth set-password`).

Never imported by the HTTP application - there is no `/signup` or password API (see
docs/architecture/auth-session-v1.md, "Credential provisioning").
"""
